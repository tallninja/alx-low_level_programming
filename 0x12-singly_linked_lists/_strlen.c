#include "lists.h"

/**
 * _strlen - calculate the length of a string
 * @str: the string;
 * Return: length of the string
 */

int _strlen(char *str)
{
	int i, len = 0;

	for (i = 0; str[i] != '\0', i++)
		len++

	return (len);
}
